# scattack

## Development

### Setup

Ensure that you have Python 3.10 or higher installed.

```bash
cd scattack
python -m venv .venv
source .venv/bin/activate
pip install -e .[test]
python -m scattack.gui
```
